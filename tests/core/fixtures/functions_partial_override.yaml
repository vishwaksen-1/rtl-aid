# Partial override - only override $clog2, inherit others from packaged
functions:
  $clog2:
    description: "Override packaged $clog2 with custom implementation"
    python_func: "int(math.ceil(math.log2(max(1, x))))"
    library: "math"
    category: "math"
    priority: 5

metadata:
  version: "1.0"
  note: "Only $clog2 is overridden; $bits, $size, $high come from packaged config"
